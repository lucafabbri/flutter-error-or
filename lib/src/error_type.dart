/// This Dart code snippet is defining an enumeration called `ErrorType` with several possible values:
/// `failure`, `unexpected`, `validation`, `conflict`, `notFound`, `unauthorized`, and `forbidden`. This
/// enum can be used to represent different types of errors in a program, making it easier to handle and
/// differentiate between various error scenarios.
enum ErrorType {
  failure,
  unexpected,
  validation,
  conflict,
  notFound,
  unauthorized,
  forbidden,
}
